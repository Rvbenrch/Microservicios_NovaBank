server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file://${user.home}/novabank-config-repo
          default-label: master

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
